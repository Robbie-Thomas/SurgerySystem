CREATE TABLE Staff (
Staff_Id INT NOT NULL AUTO_INCREMENT,
Doctor_Id INT NOT NULL,
Nurse_Id INT NOT NULL,
Row_Create DATETIME NOT NULL,
Row_LastUpdate DATETIME,
PRIMARY KEY (  Staff_Id),
FOREIGN KEY (Doctor_Id)
REFERENCES Doctor (Doctor_Id)
ON UPDATE CASCADE ON DELETE RESTRICT,
FOREIGN KEY (Nurse_Id)
REFERENCES Nurse (Nurse_Id)
ON UPDATE CASCADE ON DELETE RESTRICT,
INDEX StaffDoc(Staff_Id, Doctor_Id),
INDEX StaffNurse(Staff_Id, Nurse_Id)
) ENGINE=INNODB;

CREATE TABLE Doctor (
Doctor_Id INT NOT NULL AUTO_INCREMENT,
First_Name VARCHAR (55),
Last_Name VARCHAR (55),
Specialist_Area VARCHAR (255),
Workday_Id INT,
Row_Create DATETIME NOT NULL,
Row_LastUpdate DATETIME,
FOREIGN KEY( Workday_Id)
REFERENCES Workday(Worday_Id)
ON UPDATE CASCADE ON DELETE CASCADE,
PRIMARY KEY( Doctor_Id)
)ENGINE=INNODB;

CREATE TABLE Nurse (
Nurse_Id INT NOT NULL AUTO_INCREMENT,
First_Name VARCHAR (55),
Last_Name VARCHAR (55),
Specialist_Area VARCHAR (255),
Workday_Id INT,
Row_Create DATETIME NOT NULL,
Row_LastUpdate DATETIME,
FOREIGN KEY( Workday_Id)
REFERENCES Workday(Worday_Id)
ON UPDATE CASCADE ON DELETE CASCADE,
PRIMARY KEY( Nurse_Id)
)ENGINE=INNODB;

CREATE TABLE Workday(
  Worday_Id INT NOT NULL AUTO_INCREMENT,
  Monday TINYINT,
  Tuesday TINYINT,
  Wednesday TINYINT,
  Thursday TINYINT,
  Frday TINYINT,
  Saturday TINYINT,
  Sunday TINYINT,
  PRIMARY KEY (Worday_Id)
)ENGINE=INNODB;